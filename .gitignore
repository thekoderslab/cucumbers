# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — .env.example is intentionally committed, it holds no secrets
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# allowlist submissions captured in local dev (not used on Vercel — fs is ephemeral there)
/data/allowlist.json
